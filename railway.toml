[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && python manage.py collectstatic --noinput && gunicorn election_assistant.wsgi --log-file -"
healthcheckPath = "/"
restartPolicyType = "on_failure"
